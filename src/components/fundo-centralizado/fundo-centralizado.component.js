import React from "react"
import "./fundo-centralizado.component.style.css"

const FundoCentralizado = (url) => (
    retorno.push(<div className="background-div">
        <img className="background" src={url}></img>
        <div>{this.props.children}</div>
    </div>)
)

export default FundoCentralizado